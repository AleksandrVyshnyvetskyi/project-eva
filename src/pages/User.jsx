import { ToastContainer } from "react-toastify";
import UserInfo from "../components/user/UserInfo";

const User = () => {
    return (
        <>
            <UserInfo />
            <ToastContainer />
        </>
    );
};

export default User;
